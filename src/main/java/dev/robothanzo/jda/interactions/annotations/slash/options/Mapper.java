package dev.robothanzo.jda.interactions.annotations.slash.options;

/**
 * {@link IMapper} annotated with this annotation will be registered
 * as a mapper to map a custom {@link Option} type at startup
 */
public @interface Mapper {
}
